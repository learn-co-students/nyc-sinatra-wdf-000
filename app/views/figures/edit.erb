Edit Figure <%= @figure.name%>

<form action="/figures/<%= @figure.id%>" method="POST">
<input type="hidden" name="_method" value="patch">
Figure New Name: <input type="text" name="figure[name]" id = "figure_name">
  <br>
Figure New Titles:
 <br>
<input type="text" name="title[name]" id = "new_title">
  <br>
<% Title.all.each do |title| %>
      <label><%= title.name %></label>
      <input type="checkbox" id="title_<%=title.id%>" name="figure[title_ids][]" value="<%= title.id %>" <%= 'checked' if @figure.titles.include?(title)%>>
      <%= title.name%>
    <% end %>
      <br>
Landmarks:
  <br>
Landmark Name: <input type="text" name="landmark[name]" id = "new_landmark">
  <br>
<% Landmark.all.each do |landmark| %>
      <label><%= landmark.name %></label>
      <input type="checkbox" id="landmark_<%=landmark.id%>" name="figure[landmark_ids][]" value="<%= landmark.id %>"><br>
    <% end %>
  <input type="submit" value="Edit Figure">

</form>
